# 🏗️ Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     YOUR USERS                              │
│                                                             │
│   ┌──────────────┐              ┌──────────────┐          │
│   │  Web Browser │              │ Mobile App   │          │
│   │  (React)     │              │ (React Native)│         │
│   └──────┬───────┘              └──────┬───────┘          │
│          │                             │                   │
└──────────┼─────────────────────────────┼───────────────────┘
           │                             │
           │    HTTP/HTTPS              │    HTTP/HTTPS
           │    REST API                │    REST API
           │                             │
           └──────────────┬──────────────┘
                          │
                          ▼
           ┌──────────────────────────────┐
           │   Node.js + Express Backend  │
           │   (NO CHANGES NEEDED!)       │
           ├──────────────────────────────┤
           │  • JWT Authentication        │
           │  • REST API Endpoints        │
           │  • Business Logic            │
           └──────────────┬───────────────┘
                          │
                          ▼
           ┌──────────────────────────────┐
           │     MongoDB Database         │
           ├──────────────────────────────┤
           │  • Users Collection          │
           │  • People Collection         │
           │  • Transactions Collection   │
           │  • FriendRequests Collection │
           └──────────────────────────────┘
```

## Mobile App Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        App.tsx                              │
│                    (Entry Point)                            │
│                                                             │
│  ┌─────────────────────┐      ┌─────────────────────┐     │
│  │   AuthScreen        │      │   AppNavigator      │     │
│  │   (Login/Signup)    │      │   (Main App)        │     │
│  └─────────────────────┘      └──────────┬──────────┘     │
│                                           │                 │
└───────────────────────────────────────────┼─────────────────┘
                                            │
                    ┌───────────────────────┴───────────────────┐
                    │                                           │
                    ▼                                           ▼
    ┌───────────────────────────┐           ┌──────────────────────────┐
    │   Bottom Tab Navigator    │           │   Stack Navigator        │
    ├───────────────────────────┤           ├──────────────────────────┤
    │  • People Tab             │◄──────────│  • HomeScreen (List)     │
    │  • Profile Tab            │           │  • PersonDetailScreen    │
    └───────────────────────────┘           │  • AddPersonScreen       │
                                            └──────────────────────────┘
```

## Data Flow - User Login

```
┌──────────────┐
│ AuthScreen   │
│ User enters  │
│ credentials  │
└──────┬───────┘
       │
       │ handleLogin(email, pass)
       ▼
┌──────────────────────┐
│  api.login()         │
│  - POST /api/auth    │
└──────┬───────────────┘
       │
       │ fetch()
       ▼
┌──────────────────────┐
│  Backend Server      │
│  - Verify password   │
│  - Generate JWT      │
└──────┬───────────────┘
       │
       │ { token, user }
       ▼
┌──────────────────────┐
│  AsyncStorage        │
│  - Save user data    │
│  - Save JWT token    │
└──────┬───────────────┘
       │
       │ setIsAuthenticated(true)
       ▼
┌──────────────────────┐
│  AppNavigator        │
│  Show main app       │
└──────────────────────┘
```

## Data Flow - Add Transaction

```
┌────────────────────┐
│ PersonDetailScreen │
│ User enters        │
│ transaction        │
└────────┬───────────┘
         │
         │ handleAddTransaction()
         ▼
┌────────────────────────────┐
│  api.addTransaction()      │
│  - POST /api/transactions  │
└────────┬───────────────────┘
         │
         │ fetch() with JWT token
         ▼
┌────────────────────────────┐
│  Backend Server            │
│  - Verify JWT              │
│  - Create transaction      │
│  - Save to MongoDB         │
└────────┬───────────────────┘
         │
         │ { transaction object }
         ▼
┌────────────────────────────┐
│  Update local state        │
│  - Add to transactions[]   │
│  - Recalculate balance     │
│  - Re-render UI            │
└────────────────────────────┘
```

## Storage Architecture

### Web App (localStorage)
```
Browser
  └── localStorage
      └── "hisab-kitab-user"
          └── { id, name, email, token }
```

### Mobile App (AsyncStorage)
```
Mobile Device
  └── AsyncStorage (SQLite)
      └── "hisab-kitab-user"
          └── { id, name, email, token }
```

## Navigation Flow

```
App Launch
    │
    ▼
Check AsyncStorage
for user token
    │
    ├─── Token exists ──────► AppNavigator (Main App)
    │                             │
    │                             ├─ Bottom Tabs
    │                             │  ├─ People Tab
    │                             │  │   └─ HomeStack
    │                             │  │       ├─ HomeScreen (List)
    │                             │  │       ├─ PersonDetailScreen
    │                             │  │       └─ AddPersonScreen
    │                             │  │
    │                             │  └─ Profile Tab
    │                             │      └─ ProfileScreen
    │                             │
    │                             └─ Logout → Clear AsyncStorage
    │                                          │
    └─── No token ──────────────────────────────┘
         │                                      │
         ▼                                      ▼
    AuthScreen                           Back to AuthScreen
    (Login/Signup)
```

## API Integration Pattern

```
┌─────────────────────────────────────────────────────────┐
│                  Mobile Component                       │
│  (HomeScreen, PersonDetailScreen, etc.)                 │
└─────────────────────┬───────────────────────────────────┘
                      │
                      │ Import api
                      ▼
┌─────────────────────────────────────────────────────────┐
│                   src/api/index.ts                      │
├─────────────────────────────────────────────────────────┤
│  Functions:                                             │
│  • login(), register()                                  │
│  • getPeople(), addPerson(), deletePerson()             │
│  • addTransaction(), updateTransaction()                │
│  • getFriendRequests(), sendFriendRequest()             │
│                                                         │
│  Helpers:                                               │
│  • getStoredUser() - Read from AsyncStorage             │
│  • setStoredUser() - Write to AsyncStorage              │
│  • authHeaders() - Get JWT for requests                 │
│  • request() - Generic fetch wrapper                    │
└─────────────────────┬───────────────────────────────────┘
                      │
                      │ fetch()
                      ▼
┌─────────────────────────────────────────────────────────┐
│              Backend REST API                           │
│  http://10.0.2.2:5000/api                              │
├─────────────────────────────────────────────────────────┤
│  • POST   /api/auth/login                              │
│  • POST   /api/auth/register                           │
│  • GET    /api/people                                  │
│  • POST   /api/people                                  │
│  • DELETE /api/people/:id                              │
│  • GET    /api/transactions/:personId                  │
│  • POST   /api/transactions/:personId                  │
│  • PUT    /api/transactions/:id                        │
│  • DELETE /api/transactions/:id                        │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│                 MongoDB Database                        │
└─────────────────────────────────────────────────────────┘
```

## File Dependency Graph

```
App.tsx
  ├── src/screens/AuthScreen.tsx
  │     └── src/api/index.ts
  │           └── src/types/index.ts
  │           └── src/config.ts
  │
  └── src/navigation/AppNavigator.tsx
        ├── src/screens/HomeScreen.tsx
        │     └── src/api/index.ts
        │
        ├── src/screens/PersonDetailScreen.tsx
        │     ├── src/api/index.ts
        │     └── src/types/index.ts
        │
        ├── src/screens/AddPersonScreen.tsx
        │     └── src/api/index.ts
        │
        └── src/screens/ProfileScreen.tsx
              ├── src/api/index.ts
              └── src/types/index.ts
```

## Security Flow

```
┌──────────────────────┐
│  User Login Request  │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  Backend validates   │
│  credentials         │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  Generate JWT token  │
│  (expires in 7 days) │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  Save to AsyncStorage│
│  on mobile device    │
└──────────┬───────────┘
           │
           ▼
┌────────────────────────────┐
│  All subsequent API calls  │
│  include JWT in header:    │
│  Authorization: Bearer xxx │
└────────────┬───────────────┘
             │
             ▼
┌────────────────────────────┐
│  Backend verifies JWT      │
│  before processing request │
└────────────────────────────┘
```

## Component Hierarchy

```
App
 │
 ├─ AuthScreen (if not authenticated)
 │   ├─ TextInput (email)
 │   ├─ TextInput (password)
 │   ├─ TouchableOpacity (login button)
 │   └─ TouchableOpacity (switch to signup)
 │
 └─ AppNavigator (if authenticated)
     │
     └─ TabNavigator
         │
         ├─ Home Tab
         │   └─ StackNavigator
         │       ├─ HomeScreen
         │       │   ├─ FlatList
         │       │   │   └─ TouchableOpacity (person card)
         │       │   └─ TouchableOpacity (FAB button)
         │       │
         │       ├─ PersonDetailScreen
         │       │   ├─ Header (name, balance)
         │       │   ├─ TransactionForm (optional)
         │       │   ├─ FlatList (transactions)
         │       │   └─ TouchableOpacity (FAB button)
         │       │
         │       └─ AddPersonScreen
         │           ├─ TextInput (name)
         │           └─ TouchableOpacity (add button)
         │
         └─ Profile Tab
             └─ ProfileScreen
                 ├─ User info display
                 ├─ Menu items
                 └─ TouchableOpacity (logout button)
```

## Build Process Flow

```
Source Code (TypeScript/JSX)
         │
         ▼
    Babel Transpile
    (babel.config.js)
         │
         ▼
    Metro Bundle
    (metro.config.js)
         │
         ├─────────────────┬─────────────────┐
         ▼                 ▼                 ▼
    JavaScript      Android APK        iOS IPA
    Bundle          (Gradle)           (Xcode)
         │                 │                 │
         ▼                 ▼                 ▼
    Web Browser      Android Device    iOS Device
```

## Deployment Options

```
Development
    │
    ├─ Android Emulator ─────► npm run android
    ├─ iOS Simulator ────────► npm run ios
    └─ Physical Device ──────► USB Debug
                                   │
                                   ▼
Production
    │
    ├─ Android ──────► Build APK ──────► Play Store
    │                   │
    │                   └──────────────► Direct APK
    │
    └─ iOS ──────────► Build IPA ──────► App Store
                        (macOS only)
```

---

## Quick Reference: Key Technologies

| Layer | Technology | Purpose |
|-------|-----------|---------|
| UI | React Native | Native mobile components |
| Navigation | React Navigation | Screen routing |
| State | React Hooks | Component state |
| Storage | AsyncStorage | Persistent data |
| Network | Fetch API | HTTP requests |
| Auth | JWT | Token-based auth |
| Backend | Express.js | REST API server |
| Database | MongoDB | Data persistence |

---

**This architecture ensures:**
- ✅ Clean separation of concerns
- ✅ Reusable components
- ✅ Secure authentication
- ✅ Offline data persistence
- ✅ Scalable structure
